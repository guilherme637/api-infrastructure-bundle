{
  "name": "guichaves/api-infrastructure-bundle",
  "description": "Bundle para projetos pessoais",
  "minimum-stability": "dev",
  "license": "proprietary",
  "authors": [
    {
      "name": "guilherme",
      "email": "email@example.com"
    }
  ],
  "require": {
    "php": "8.*"
  },
  "scripts": {
    "install": {
      "install-adapter": "composer install -d adapter/",
      "install-exception-response": "composer install -d response-exception/"
    },
    "post-install-cmd": [
      "@install"
    ]
  }
}